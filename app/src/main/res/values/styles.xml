<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="loading_dialog" parent="android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    android:layout_weight="1"
    android:background="@drawable/bt_rechange_bg_select"
    android:textSize="17dp"
    android:text="充值50元"
    android:layout_width="250dp"
    android:layout_height="0dp"/>

    <style name="RechangeButtonStytle" parent="Base.Widget.AppCompat.Button">
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textSize">17dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_width">200dp</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:background">@drawable/bt_rechange_bg_select</item>
        <item name="android:layout_marginBottom">15dp</item>
    </style>
  <!--  android:layout_marginLeft="20dp"
    android:textSize="22sp"
    android:text="余额:"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"/>-->
    
    <style name="TextSmallStyle" parent="TextAppearance.AppCompat">
        <item name="android:textSize">20sp</item>
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>

    </style>

</resources>
